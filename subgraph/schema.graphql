type Balance @entity {
    # -- balance --
    contract: String!
    owner: String!
    balance: BigInt!

    # -- block --
    timestamp: Timestamp!
    block_num: BigInt!
    date: String!

    # -- primary key --
    id: ID!
}
